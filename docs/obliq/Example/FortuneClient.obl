(* A client for FortuneServer. *)

module FortuneClient;

let fortune = net_import("fortune","");

(* 
  fortune.learn(
     "I am sure we should all be as happy as kind. "
     & "The world is so full of a number of things");
   fortune.tell;
*)